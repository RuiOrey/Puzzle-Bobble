import TWEEN from '@tweenjs/tween.js';

import * as THREE from 'three';

export const hasCollisions = ( object, shape ) => {

    // checkBoxWithBoxCollision

    ColliderType = {
     // sphere:
    }

    let state = {
        // type:
    };

    return { collision: state };
};